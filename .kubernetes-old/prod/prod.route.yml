apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prod-route
  namespace: prod
spec:
  parentRefs:
    - name: shared-gateway
      namespace: shared
  hostnames:
    - "f19.dawidbladek0831.org"
  rules:
    - backendRefs:
        - name: hello-svc
          port: 80